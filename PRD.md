# PRD — 보안용어 쉬운 비유 설명 서비스

## 1) 목표와 성공 조건

**목표**
보안 전공 초보자 및 교육생이 전공 서적이나 긴 문서를 찾아볼 필요 없이, 생소한 보안 용어를 검색했을 때 일상적인 비유를 통해 빠르고 직관적으로 이해할 수 있도록 정의·비유·역할을 3초 이내에 제공한다.

**성공 조건**
- 사용자가 용어를 입력하고 3초 이내에 설명(정의·비유·역할)이 화면에 출력된다.
- 별도 회원가입, 로그인, 결제 없이 누구나 즉시 사용할 수 있다.
- 단일 화면에서 검색부터 결과 확인까지 완료된다.

---

## 2) 사용자 스토리

- 보안 전공 초보자로서, 전공 서적이나 긴 논문을 뒤지지 않고도 낯선 용어(예: "제로데이")를 검색해서 즉시 감을 잡고 싶다.
- 보안 교육생으로서, 수업 중 처음 들은 용어를 그 자리에서 검색해 일상적인 비유로 빠르게 이해하고 싶다.
- 보안 전공 초보자로서, 어려운 정의를 그대로 외우기보다 비유를 통해 "왜 이게 중요한지" 직관적으로 파악하고 싶다.

---

## 3) 화면 명세

**단일 화면 구성 (1 Screen)**

1. **검색 영역**
   - 텍스트 입력창 1개 (플레이스홀더: 예 "궁금한 보안 용어를 입력하세요 (예: 피싱, VPN)")
   - 검색 버튼 1개 (또는 Enter 키로 검색 실행)

2. **결과 영역** (검색 실행 후 표시)
   - 검색한 용어명 (제목)
   - ① 한 줄 정의
   - ② 일상 상황에 접목한 비유 설명
   - ③ 실제 보안에서의 역할 설명
   - 다시 검색할 수 있는 입력창은 그대로 상단에 유지

3. **상태 표시 영역**
   - 로딩 중 표시 (스피너 또는 텍스트 "설명을 찾고 있어요…")
   - 에러/안내 메시지 표시 영역 (결과 영역과 같은 자리에 대체 표시)

화면 전환 없음. 모든 상호작용은 이 한 화면 내에서 발생.

---

## 4) 기능 명세

**기능: 보안 용어 검색 및 설명 출력 (단일 기능)**

- 사용자가 입력창에 보안 관련 용어(단어/짧은 구)를 입력하고 검색을 실행한다.
- 시스템은 입력된 용어를 해석하여 아래 3가지 요소로 구성된 설명을 생성/조회한다.
  1. 한 줄 정의
  2. 일상 상황에 접목한 비유 설명
  3. 실제 보안에서의 역할 설명
- 생성된 설명을 결과 영역에 출력한다.
- 사용자는 언제든 새로운 검색어를 입력해 같은 화면에서 재검색할 수 있다.
- 이전 검색 결과는 저장되지 않는다 (세션 종료 시 소멸, 새로고침 시 초기화).

**포함하지 않는 것 (재확인)**
- 로그인/계정, 결제, DB 저장 없음
- 검색 이력 저장, 즐겨찾기, 공유 기능 없음
- 용어 목록 브라우징(카테고리, 리스트) 없음 — 오직 "검색" 한 가지 진입 경로만 존재

---

## 5) 예외 처리

이 서비스는 사용자 입력과 설명 생성 과정에서 발생 가능한 예외를 아래와 같이 정의한다. 각 항목은 **발생 조건 → 시스템 동작 → 사용자에게 보이는 메시지/UI → 후속 행동** 순으로 기술한다.

### 5-1. 입력이 비어있는 경우
- **발생 조건**: 사용자가 입력창에 아무것도 입력하지 않고 검색을 실행(버튼 클릭 또는 Enter)했을 때.
- **시스템 동작**: 검색 요청 자체를 실행하지 않는다 (설명 생성 로직 호출 금지).
- **사용자 화면**: 입력창 하단 또는 근처에 "검색어를 입력해주세요" 안내 문구 표시. 입력창 포커스 유지.
- **후속 행동**: 사용자가 입력 후 재검색.

### 5-2. 존재하지 않거나 보안과 무관한 용어인 경우
- **발생 조건**: 입력값이 보안 용어가 아니거나("사과", "날씨" 등), 시스템이 의미 있는 설명을 생성할 수 없다고 판단한 경우.
- **시스템 동작**: 설명을 억지로 생성하지 않고, 판단 불가/무관 상태로 처리.
- **사용자 화면**: "'{입력값}'은(는) 보안 용어로 보이지 않아요. 다른 용어로 다시 검색해보세요." 같은 안내 메시지 표시. 예시 용어(피싱, VPN 등) 2~3개를 함께 노출해 재검색 유도.
- **후속 행동**: 사용자가 예시 용어 클릭 또는 직접 재입력.

### 5-3. 오탈자가 발생한 경우
- **발생 조건**: 입력값이 실제 보안 용어와 유사하지만 철자가 틀린 경우 (예: "피슁", "랜섬웨어" → "렌섬웨어").
- **시스템 동작**: 정확히 일치하는 용어가 없더라도, 유사도가 높은 용어가 있다고 판단되면 해당 용어에 대한 설명을 대신 생성하거나 추천한다.
- **사용자 화면**: 두 가지 방식 중 하나로 표시
  - (a) 자동 보정: "'{입력값}' 대신 '{추정용어}'의 설명을 보여드려요" 안내 후 결과 출력
  - (b) 확인 요청: "혹시 '{추정용어}'를 찾으시나요?" 버튼 형태로 제시, 클릭 시 해당 결과 출력
- **후속 행동**: 사용자가 추천 결과를 확인하거나, 원하는 결과가 아니면 재검색.

### 5-4. 설명 생성(AI 응답)이 실패한 경우
- **발생 조건**: 설명을 만들어내는 처리 과정 자체가 오류로 중단되거나 응답을 받지 못한 경우 (네트워크 오류, 서버 오류 등 원인 불문).
- **시스템 동작**: 부분적이거나 손상된 결과를 화면에 노출하지 않는다. 실패를 명확히 감지하고 결과 영역을 에러 상태로 전환.
- **사용자 화면**: "설명을 불러오지 못했어요. 다시 시도해주세요." 메시지와 함께 "다시 시도" 버튼 표시.
- **후속 행동**: 사용자가 "다시 시도" 버튼으로 동일 검색어 재요청, 또는 새로운 검색어 입력.

### 5-5. 응답 시간이 오래 걸리는 경우
- **발생 조건**: 설명 생성 요청 후 일정 시간(예: 3초 이상)이 지나도 결과가 도착하지 않는 경우.
- **시스템 동작**: 사용자가 대기 상태임을 인지할 수 있도록 로딩 표시를 유지. 일정 시간(예: 10초 이상)을 초과하면 타임아웃으로 간주해 실패 처리(5-4와 동일한 안내로 전환).
- **사용자 화면**: 로딩 중 "설명을 찾고 있어요…" 표시 → 임계 시간 초과 시 "응답이 지연되고 있어요. 다시 시도해주세요." + 재시도 버튼으로 전환.
- **후속 행동**: 사용자가 재시도하거나 대기를 포기하고 다른 검색어 입력.

### 5-6. 결과가 이상하거나 이해가 안 되는 경우 (재시도 요구)
- **발생 조건**: 설명은 정상적으로 출력되었으나, 사용자가 보기에 부정확하거나 이해하기 어렵다고 느끼는 경우.
- **시스템 동작**: 동일한 검색어에 대해 재생성을 요청할 수 있는 기능을 결과 영역에 상시 제공한다.
- **사용자 화면**: 결과 하단에 "다른 설명으로 다시 보기" 또는 "재시도" 버튼 상시 노출. 클릭 시 동일 용어에 대한 설명을 새로 생성하여 기존 결과를 대체.
- **후속 행동**: 사용자가 만족할 때까지 반복 재시도 가능 (횟수 제한은 정책으로 별도 정의 가능하나 기본 무제한).

> 예외 처리 공통 원칙: 어떤 예외 상황에서도 사용자는 입력창을 통해 즉시 새로운 검색을 시도할 수 있어야 하며, 에러 상태가 화면 전체를 막거나 재검색을 방해해서는 안 된다.

---

## 6) 완료 조건

- 단일 화면에서 검색 입력 → 결과 출력(정의/비유/역할 3요소)까지의 전체 흐름이 동작한다.
- 로그인, 결제, DB 저장 기능이 존재하지 않는다.
- 위 5)에 명시된 6가지 예외 상황이 각각 정의된 대로 사용자에게 안내되고, 모든 예외 상황에서 재검색이 가능하다.
- 정상 케이스 기준, 용어 입력 후 3초 이내에 결과가 출력된다 (지연 시 5-5 기준에 따라 처리됨을 확인).